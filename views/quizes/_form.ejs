<label for="preg" class="rem">Pregunta:</label>
<input type="text" id="preg" name="quiz[pregunta]" value="<%= quiz.pregunta %>" /> <br />
<label for="resp" class="rem">Respuesta:</label>
<input type="text" id="resp" name="quiz[respuesta]" value="<%= quiz.respuesta %>" /> <br />

<label for="tem" class="rem">Tema:</label>
<select id=""tem" name="quiz[tema]" value="<%= quiz.tema %>">
<%
	var valores = [
		{clave: 'otro', texto: 'Otro'},
		{clave: 'humanidades', texto: 'Humanidades'},
		{clave: 'ocio', texto: 'Ocio'},
		{clave: 'ciencia', texto: 'Ciencia'},
		{clave: 'tecnologia', texto: 'Tecnología'}
	];
	for (var i in valores) {
%>
	<option value="<%= valores[i].clave %>" 
		<% if (valores[i].clave === quiz.tema ) { %>
			selected
		<% } %>
	>
		<%= valores[i].texto %>
	</option>
<%
	};
%>
</select>
<br />
<a href="/quizes"><button type="button">Cancelar</button></a>
<input type="submit" value="Salvar" />
